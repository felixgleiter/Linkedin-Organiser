def input(data):

  data = raw_input(str(data) + ": ").lower()

def single_surname_list_check(lst):

 for key in lst:
        
        if Surname == lst[key].lower():
          print key + lst[key]
          
        elif Surname == "":
          print key + lst[key]
