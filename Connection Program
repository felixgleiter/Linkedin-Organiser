"""CREATE TABLE connections (id INTEGER, first_name TEXT, last_name TEXT, email_address TEXT, company TEXT, position TEXT, connected_on TEXT, tags TEXT);

# Converting the CSV file into an SQL table

SET count = 1
while count <= 5000:
INSERT INTO connections (id, first_name, last_name, email_address, company, position, connected_on, tags) VALUES (count, 'fn', 'ln', 'ea', 'com', 'pos', 'cn', 'ta');
count += 1"""

# Data Lists for Companies, Positions etc.

blattler = {'Daniel ' : 'Blattler', 'Nathalie ' : 'Breuer', 'Mrinal ' : 'Madina', 'David ' : 'Brown', 'Stephan ' : 'Hammes', 'Nina ' : 'Schu'}
Companies = {blattler : 'blattler'}
directors = {'Daniel ' : 'Blattler'}
project_managers = {'Nathalie ' : 'Breuer', 'Nina ' : 'Schu'}
Positions = [directors, project_managers]
  
# Basic Query for the SQL table

def Query():
  
  print "Hello! Please leave any unknown values blank."
  
  Company = list(raw_input("Company: ").lower())
  
  for c in Companies:
    
    if Company == Companies[c]: # Why do these not match up?!
      Position = raw_input("Position: ").lower()
    
      if Position == "director":
        Surname = raw_input("Surname: ").lower()
      
        for key in c and directors:
      
          if Surname == c[key].lower():
            print key + c[key]
    
      elif Position == "project manager":
        Surname = raw_input("Surname: ").lower()
      
        for key in c and project_managers:
      
          if Surname == c[key].lower():
            print key + c[key]
          
          elif Surname == "":
          
            print key + project_managers[key]

      elif Position == "":
        Surname = raw_input("Surname: ").lower()
      
        for key in c:
        
          if Surname == c[key].lower():
            print key + c[key]
        
          elif Surname == "":
            print key + c[key]
          
    elif Company == "":
      Position = raw_input("Position: ").lower()
    
      if Position == "director":
        Surname = raw_input("Surname: ").lower()
      
        for key in directors:
        
          if Surname == directors[key].lower():
            print key + directors[key]
          
          elif Surname == "":
          
            for key in directors:
              print key + directors[key]
            
      elif Position == "project manager":
        Surname = raw_input("Surname: ").lower()
      
        for key in project_managers:
        
          if Surname == project_managers[key].lower():
            print key + project_managers[key]
          
          elif Surname == "":
            print key + project_managers[key]
            
      elif Position == "":
        Surname = raw_input("Surname: ").lower()
      
        for c in Companies:
        
          for key in c:
          
            if Surname == c[key].lower():
              print key + c[key]
            
            elif Surname == "":
              return "I can't help you if you don't give me any information."
          
    else:
      return "I'm afraid I don't know that company. Please restart and try again."

print Query()
